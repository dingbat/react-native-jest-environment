Feature: Render React Native Components
  If all goes well
  We should be able to render a component

  Scenario: Render
    Given a component
    Then it has a property
    And I can change a native property
    And I can test the change
